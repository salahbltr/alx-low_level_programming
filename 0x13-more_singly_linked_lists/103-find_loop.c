#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * *find_listint_loop -  finds the loop in a linked list.
 * @head: the head of listint
 * Return: The@ of the node where the loop starts,or NULL if there is noloop
 */
listint_t *find_listint_loop(listint_t *head)
{
}
